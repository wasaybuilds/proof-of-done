# `.only` added — sibling tests silently stop running

Pattern from the rule catalogue (docs/DETECTION-RULES.md, POD002): adding `.only` makes the runner skip every other test in the file while still reporting green.
