# Real test deleted, new test padded with assertions that can't fail

The loophole documented for POD001/POD003: delete a real test and add enough trivial assertions to look like a refactor. Vacuous assertions don't count as test strength, so this blocks.
